apiVersion: apps/v1
kind: Deployment
metadata:
  name: admin
  namespace: appointy
spec:
  replicas: 1
  selector:
    matchLabels:
      app: admin
  template:
    metadata:
      labels:
        app: admin
    spec:
      containers:
        - name: admin
          image: docker.io/${DOCKERHUB_USERNAME}/appointy-admin:${IMAGE_TAG}
          imagePullPolicy: Always
          ports:
            - containerPort: 80
          env:
            - name: BACKEND_UPSTREAM
              value: http://backend-service:4000
          readinessProbe:
            httpGet:
              path: /healthz
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /healthz
              port: 80
            initialDelaySeconds: 15
            periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: admin-service
  namespace: appointy
spec:
  selector:
    app: admin
  ports:
    - name: http
      port: 80
      targetPort: 80
      nodePort: 30081
  type: NodePort
